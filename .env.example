# Copie este arquivo para ".env.local" e preencha para habilitar a IA real.
# Sem esta chave, o sistema funciona em "modo simulado" (resumos e minutas de exemplo).

# Chave de API da Anthropic (Claude) — obtenha em https://console.anthropic.com
ANTHROPIC_API_KEY=
